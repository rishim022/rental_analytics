# Copy to .env and adjust as needed. .env is gitignored.

# Avoids file-permission issues for mounted volumes on Linux hosts (harmless on macOS).
AIRFLOW_UID=50000

# dev | prod -- selects the BigQuery dataset + dbt target via airflow/dags/common/config.py
RENTAL_ANALYTICS_ENV=dev
